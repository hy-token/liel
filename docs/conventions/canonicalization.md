# Canonicalization conventions (lightweight)

This page is kept for compatibility. Canonicalization guidance has been merged
into `recommended-labels.md`.

## See instead

- Naming and normalization rules: `recommended-labels.md` ->
  "Minimal naming and normalization rules"
- Identity-key guidance: `recommended-labels.md` ->
  "Minimal identity-key rules"
- Evidence workflow: `provenance.md`

